@using Microsoft.AspNet.Identity
@using Stagio.Domain.Application
<ul class="nav navbar-nav">

    @if (Request.IsAuthenticated && User.IsInRole(RoleName.Student))
    {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" id="student-menu">Student <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Inscrire une date d'entrevue", MVC.Interview.Create(), new { id = "add-entrevue" }) </li>
                <li>@Html.ActionLink("Voir les stages avec postulation", MVC.Student.ApplyList())</li>          
            </ul>
        </li>
    }

    

</ul>
